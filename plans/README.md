# Implementation Plans

Generated by the improve + improve-animations skills on 2026-07-13 at commit `76c7142`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline (Vitest + easing characterization) | P1 | L | — | TODO |
| 002 | Unify preview/export animation math | P1 | M | 001 | TODO |
| 003 | Slim undo snapshots + debounce IndexedDB writes | P1 | M | — (001 recommended) | TODO |
| 004 | Isolate 60fps playback from app-wide re-renders | P2 | L | 001, ideally 002 | TODO |
| 005 | Wallpaper picker thumbnails | P2 | M | — | TODO |
| 006 | SPIKE: project files (.framey.json) + motion presets | P2 | M | 003 helps | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Dependency notes

- 002 requires 001: the characterization tests are the only guard proving preview/export math didn't change during unification.
- 004 is easiest after 002 (`computeLayerFrame` is the per-frame function the imperative loop calls).
- 006's schema should reuse 003's media/state separation helpers.

## Fixed directly (not planned — already implemented at audit time)

These audit findings were implemented straight away in the polish pass following the audit (see git history after `76c7142`):

- Export hardening: WebCodecs feature-detect, visible error state, `finally` cleanup of encoder/video elements, `toBlob` null guard (was: silent failure + leaks)
- Upload robustness: `onerror` on every FileReader/Image/video so one corrupt file can't hang a batch
- Stale keyboard-zoom closures (missing effect deps)
- Lazy `mp4-muxer` import; removed unused `tailwindcss` + `@tailwindcss/vite` deps
- Deleted dead `renderBgToCanvas`
- Lint: unused imports/vars removed
- Motion hygiene (from the animations audit): explicit transition properties replacing 22 `transition: all`, strong `--ease-out` token, `prefers-reduced-motion` support, `(hover:hover)` gating, button press feedback, popover/dropdown entrance motion
- SEO/meta copy updated to list GIF/PNG/WebP export and video import

## Findings considered and rejected

- `EXIT_ANIMATION_TYPES` id `fadeIn` labeled "Fade Out" (App.jsx:43): intentional id reuse for the exit lookup; cosmetic only. Rename only if it ever causes a real bug.
- Google Analytics measurement ID in index.html: public client-side identifier, by design.
- CSP + SRI hardening (SECURITY-02): real but deferred — needs testing against inline GA bootstrap and the SVG-rasterization path; revisit after 002 stabilizes the export pipeline.
- Two color pickers (DEBT-06): consolidation deferred until one of them next needs a feature; both currently work.
